/**
 * Contract ABIs for Logistics vSaaS (indexer, frontend).
 * Regenerate from `packages/contracts/out/` after contract changes if needed.
 */

export { LogisticsFactoryAbi } from "./LogisticsFactory";
export { LogisticsZoneAbi } from "./LogisticsZone";
