{
  "name": "logistics-vsaas",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/api",
    "packages/shared",
    "packages/contracts"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "npm@10.2.0",
  "engines": {
    "node": ">=20"
  }
}
